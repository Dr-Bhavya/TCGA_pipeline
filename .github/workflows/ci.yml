name: End-to-End Pipeline Engine

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  # ==========================================================================
  # JOB 1: REMOTE EC2 PROVISIONING (Runs on GitHub Cloud Servers)
  # ==========================================================================
  setup-ec2-environment:
    runs-on: ubuntu-latest # Runs on GitHub's free nodes to configure the machine

    steps:
    - name: Checkout Code Repository
      uses: actions/checkout@v4

    - name: Execute Remote Setup over SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.TCGA_AWS_EC2_ACCESS_KEY }}
        username: ubuntu
        key: ${{ secrets.TCGA_AWS_EC2_PUBLIC_IP }}
        port: 22
        timeout: 10m
        script: |
          echo "=== 1. INSTALLING COMPUTE DEPENDENCIES ==="
          sudo apt-get update -y
          sudo apt-get install -y docker.io openjdk-17-jre-headless curl unzip

          echo "=== 2. OVERRIDING DOCKER SOCKET PERMISSIONS ==="
          sudo usermod -aG docker ubuntu
          sudo chmod 666 /var/run/docker.sock

          echo "=== 3. INITIALIZING GITHUB RUNNER AGENT ==="
          cd /home/ubuntu
          if [ ! -d "actions-runner" ]; then
            mkdir actions-runner && cd actions-runner
            curl -o actions-runner-linux-x64-3.220.0.tar.gz -L https://github.com
            tar xzf ./actions-runner-linux-x64-3.220.0.tar.gz
            
            ./config.sh --unattended \
              --url "https://github.com{{ github.repository }}" \
              --token "${{ secrets.RUNNER_REGISTRATION_TOKEN }}" \
              --name "ec2-free-runner" \
              --replace
              
            sudo ./svc.sh install
            sudo ./svc.sh start
          else
            echo "Runner already registered and operational."
          fi

  # ==========================================================================
  # JOB 2: PIPELINE INTEGRATION CHECK (Runs Directly on your Free EC2 Machine)
  # ==========================================================================
  run-nextflow-test:
    runs-on: self-hosted               # Triggers only on your private EC2 node [1]
    needs: setup-ec2-environment        # Strict block forcing this to WAIT for Job 1 to finish

    steps:
    - name: Checkout Code Repository
      uses: actions/checkout@v4

    - name: Build Local Environment Image
      run: |
        docker build -t docker_tcga_env:latest .
        docker image ls

    - name: Setup Nextflow Engine
      run: |
        if ! command -v nextflow &> /dev/null; then
          mkdir -p "$HOME/.nextflow"
          curl -fsSL get.nextflow.io | bash
          mv nextflow $HOME/.nextflow/
        fi
        echo "$HOME/.nextflow" >> $GITHUB_PATH

    - name: Execute End-to-End Integration Test
      run: |
        nextflow run main.nf -profile test -stub-run
